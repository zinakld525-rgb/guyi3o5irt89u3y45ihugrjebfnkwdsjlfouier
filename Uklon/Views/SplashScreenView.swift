import SwiftUI

struct SplashScreenView : View {
    var body: some View {
        ZStack {
            
            
            VStack {
                Spacer()
                ProgressView()
                Spacer()
            }
        }
    }
    
    
}
